{
	"name": "paperdrill-tests",
	"type": "module",
	"private": true,
	"scripts": {
		"test": "bun test integration --timeout 20000 --max-concurrency 1",
		"test:unit": "cd ../backend && set -a && . ../.env.test && set +a && bun test && cd ../engine && set -a && . ../.env.test && set +a && bun test",
		"test:integration": "cd .. && sh tests/run-integration.sh"
	},
	"dependencies": {
		"pg": "^8.20.0",
		"redis": "^6.0.0"
	},
	"devDependencies": {
		"@types/bun": "latest",
		"@types/pg": "^8.20.0",
		"typescript": "^5"
	}
}
